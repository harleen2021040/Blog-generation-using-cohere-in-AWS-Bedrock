# Blog-generation-using-cohere-in-AWS-Bedrock
This project demonstrates a serverless pipeline to generate and store blog content using Generative AI (Cohere via Amazon Bedrock), AWS Lambda, and Amazon S3. The application generates a short blog (200 words) based on any topic provided in a POST request and automatically stores the generated content in an S3 bucket.
